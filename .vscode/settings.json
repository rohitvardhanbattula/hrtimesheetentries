{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Tables",
            "database": "${workspaceFolder:hrtimesheetentries}/db.sqlite"
        }
    ]
}